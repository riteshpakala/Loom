import Granite
import GraniteUI
import SwiftUI


extension Globe: View {
    public var view: some View {
        VStack(spacing: 0) {
            mainView
            
        }
        .background(Color.background)
    }
}
